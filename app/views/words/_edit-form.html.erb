<%= form_with model: word, local: true do |f|%>

  <div class="field">
    <%= f.label :image, "プロトタイプの画像" %><br />
    <%= f.file_field :image, id:"prototype_image" %>
  </div>
  <div class="field">
    <%= f.label :name, "画像名" %><br />
    <%= f.text_field :name, id:"prototype_title" %>
  </div>

  <div class="field">
    <%= f.label :note, "言語化フォーマット" %><br />
    <%= f.text_area :note, class: :form__text, id:"prototype_catch_copy" %>
  </div>

  <div class="field">
    <%= f.label :genre_id, "ジャンル" %><br />
    <%= f.collection_select(:genre_id, Genre.all, :id, :name, {}, {class:"select-box", id:"genre-status"}) %>
  </div>



  <div class="actions">
    <%= f.submit "編集する", class: :form__btn  %>
  </div>
  <%= link_to '戻る', word_path(word.id), class: :form__btn %>
<% end %>